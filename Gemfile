#ruby=2.7.2
#ruby-gemset=jekyll-blogger-generator
source 'https://rubygems.org'
gemspec

gem 'jekyll', ENV['JEKYLL_VERSION'] ? "~> #{ENV['JEKYLL_VERSION']}" : '>= 4.0'
gem 'minima'

group :test do
  gem 'webmock'
end
